export enum TimelineBubbleType {
    Initial = 'initial',
    Default = 'default',
    Invisible = 'invisible',
    Final = 'final',
}

export enum TimelineConnectorType {
    Solid = 'solid',
    Dashed = 'dashed',
}
